baseURL = 'https://shawnstrick.land/'
languageCode = 'en-us'
title = 'Shawn Strickland'
theme = 'ananke'

[deployment]

[[deployment.targets]]
# An arbitrary name for this target.
name = "production"

# Amazon Web Services S3; see https://gocloud.dev/howto/blob/#s3
# For S3-compatible endpoints, see https://gocloud.dev/howto/blob/#s3-compatible
URL = "s3://shawn-strickland-public-site?region=us-east-1"

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]
  page = ["HTML", "RSS"] #Need all links with uglyURLs (uglyURLs = true)